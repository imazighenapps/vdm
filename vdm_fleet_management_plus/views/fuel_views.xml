<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_fuel_tree" model="ir.ui.view">
        <field name="name">fleet.fuel.list</field>
        <field name="model">fleet.fuel</field>
        <field name="arch" type="xml">
            <list>
                <field name="fuel_date"/>
                <field name="vehicle_id"/>
                <field name="driver_id"/>
                <field name="liters"/>
                <field name="price_per_liter"/>
                <field name="total_cost"/>
                <field name="odometer"/>
                <field name="fuel_type"/>
                <field name="station"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="view_fuel_form" model="ir.ui.view">
        <field name="name">fleet.fuel.form</field>
        <field name="model">fleet.fuel</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" type="object" string="Confirm" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_draft" type="object" string="Reset to Draft" invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Fuel Entry Description"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="vehicle_id"/>
                            <field name="driver_id"/>
                            <field name="fuel_date"/>
                            <field name="fuel_type"/>
                        </group>
                        <group>
                            <field name="liters"/>
                            <field name="price_per_liter"/>
                            <field name="total_cost"/>
                            <field name="odometer"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="station"/>
                            <field name="receipt_number"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Notes">
                            <field name="notes" placeholder="Fuel entry notes..."/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_fuel_search" model="ir.ui.view">
        <field name="name">fleet.fuel.search</field>
        <field name="model">fleet.fuel</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="driver_id"/>
                <filter name="filter_draft" string="Draft" domain="[('state', '=', 'draft')]"/>
                <filter name="filter_confirmed" string="Confirmed" domain="[('state', '=', 'confirmed')]"/>
                <group expand="0" string="Group By">
                    <filter name="group_vehicle" string="Vehicle" context="{'group_by': 'vehicle_id'}"/>
                    <filter name="group_driver" string="Driver" context="{'group_by': 'driver_id'}"/>
                    <filter name="group_date" string="Date" context="{'group_by': 'fuel_date'}"/>
                    <filter name="group_type" string="Fuel Type" context="{'group_by': 'fuel_type'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_fuel" model="ir.actions.act_window">
        <field name="name">Fuel</field>
        <field name="res_model">fleet.fuel</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_fuel_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first fuel entry
            </p>
            <p>Track fuel consumption and costs for your vehicles.</p>
        </field>
    </record>
</odoo>
